# M15: 集計 CLI / script MVP

## 目的

M14 で決めた入力形式を受け取り、M12 の measurement schema に従う CSV / JSON を生成する MVP を作る。

## 完了条件

- [ ] 入力 fixture を用意する
- [ ] CSV / JSON 出力を実装する
- [ ] token、duration、tool call count、error count、欠損属性、未知 span 名を確認する
- [ ] 自動テストを追加する
- [ ] 必要なレビューを `review.md` に記録する

## 検証記録

- 未実施。

